spring.application.name=foodrescue-api

# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/foodrescuedb}
# For production, you can use:
# spring.data.mongodb.uri=mongodb+srv://<username>:<password>@cluster0.mongodb.net/foodrescuedb

# Server
server.port=8080

# # H2 Console Configuration
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console
# spring.h2.console.settings.web-allow-others=true

# # Datasource Configuration (H2 In-Memory)
# spring.datasource.url=jdbc:h2:mem:foodrescuedb;DB_CLOSE_DELAY=-1
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password

# # JPA / Hibernate
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# SMTP Configuration (Gmail Example)
# IMPORTANT: If using Gmail, you MUST use an 'App Password', not your regular login password.
# 1. Enable 2-Factor Authentication in your Google Account.
# 2. Search for 'App Passwords' in Google Account settings.
# 3. Generate a 16-character password for 'Mail' and 'Other (Custom Name)'.
# 4. Paste that 16-character code (without spaces) in spring.mail.password.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:deykrunal71@gmail.com}
spring.mail.password=${MAIL_PASSWORD:pdyx hias qoxg bejs}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
